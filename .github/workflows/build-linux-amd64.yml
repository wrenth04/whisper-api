name: Build Linux AMD64 Binary

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - work
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-linux-amd64:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install pyinstaller

      - name: Build linux amd64 executable
        run: |
          pyinstaller \
            --noconfirm \
            --clean \
            --onefile \
            --name whisper-api-server \
            --collect-all openvino \
            --collect-all openvino_tokenizers \
            --collect-all openvino_genai \
            --collect-all faster_whisper \
            --hidden-import openvino \
            --hidden-import openvino.runtime \
            --hidden-import openvino_tokenizers \
            --hidden-import openvino_genai \
            --hidden-import faster_whisper \
            app/main.py

      - name: Package artifact
        run: |
          mkdir -p release
          cp dist/whisper-api-server release/
          chmod +x release/whisper-api-server
          tar -czf whisper-api-server-linux-amd64.tar.gz -C release whisper-api-server

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: whisper-api-server-linux-amd64
          path: |
            dist/whisper-api-server
            whisper-api-server-linux-amd64.tar.gz

      - name: Upload Linux binary to GitHub Release (tags only)
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: whisper-api-server-linux-amd64.tar.gz
          overwrite_files: true
          token: ${{ secrets.GITHUB_TOKEN }}
